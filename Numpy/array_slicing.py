import numpy as np

a = np.arange(20)

# For slicing array[start:end]
# Start will be exclusive. Eg: a[4:] so it will stop at element 3rd and will start printing from elementr 4th.
# Leaving empty means till end. Where as end is inclusive.

# slice Function. slice(start,end,step).
# steps are optional.